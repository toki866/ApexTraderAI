numpy
pandas
PyYAML
yfinance
matplotlib
scikit-learn
hdbscan
gymnasium
stable-baselines3
PyWavelets>=1.6

# StepB mamba runtime uses a Linux/WSL environment for mamba-ssm.
# Windows/CI setup installs requirements.txt here, then StepB preflight verifies
# torch / pywt / mamba_ssm imports in the actual runtime before execution.
# TICC clustering backends
fast-ticc==1.0.1  # primary backend (WSL mamba_cuda validated)
# TICC_solver  # optional legacy backend (not required; may be unavailable on some environments)
